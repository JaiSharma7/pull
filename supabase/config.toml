# Local development configuration. The hosted project is `pull`
# (ref zjvfwhjwaytyogdxeddo). Link with: supabase link --project-ref <ref>
project_id = "what-a-pull"

[api]
enabled = true
port = 54321
# Only `public` is exposed. The hosted project matches this, so types generated
# locally and from the hosted project are identical — which CI check 4 asserts.
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 100

[db]
port = 54322
shadow_port = 54320
major_version = 17

[db.pooler]
enabled = false

[realtime]
enabled = false

[studio]
enabled = true
port = 54323

[local_smtp]
enabled = true
port = 54324

[storage]
enabled = true
file_size_limit = "50MiB"

[auth]
enabled = true
site_url = "http://127.0.0.1:5173"
# 5174 is here because a second dev server is a normal state on this project, not
# an accident: a stale one on 5173 is easy to leave running and hard to notice.
additional_redirect_urls = ["http://127.0.0.1:5173", "http://127.0.0.1:5174"]
jwt_expiry = 3600
enable_signup = true

# A guest session: a real user row with no address attached to it.
#
# The point is the first five minutes. Onboarding is a preference row keyed to a user
# (`OnboardingGate` in Preferences.tsx), the feed is a query keyed to a user, and the
# Delta is a knowledge state keyed to a user -- so there is no way to show any of it to
# somebody who has not signed in, and asking for an address before the product has
# demonstrated anything is asking for trust on credit.
#
# `signInAnonymously` gives a real `auth.users` row, so `handle_new_user` writes the
# profile and preference rows, RLS behaves exactly as it does for anyone else, and every
# screen works without a special case. What it does NOT give is a way back in: the
# session lives in this browser's storage and nothing can restore it, which is why the
# UI says so rather than implying a guest has an account.
#
# The cost side is bounded in 20260901190000: a guest may not enqueue generation (law 2
# -- anonymous sessions are unlimited, so per-user generation quota would be no quota at
# all), may not author a summary, and may not file a report. See that migration.
# A running stack does NOT pick this up. `supabase start` against containers that are
# already up reports them and returns; it does not re-read this file. So on a machine
# where the stack was already running, the guest button fails with
# `anonymous_provider_disabled` and the app correctly reports that guest sessions are
# switched off — which looks exactly like a bug in the button. `supabase stop && supabase
# start` is the fix. Verified on 2026-09-01 by hitting precisely this.
enable_anonymous_sign_ins = true

# A one-time code is the whole credential here, so its lifetime is the window in which
# a leaked email is a working account. Ten minutes is long enough to switch apps, find
# the message and type six digits, and short enough that a code sitting in a synced
# inbox stops being useful quickly. GoTrue's default is an hour.
[auth.email]
enable_signup = true
enable_confirmations = false
otp_expiry = 600
# One code per minute per address. The sign-in screen already handles the rate-limit
# error and opens its paste-a-link escape hatch (Auth.tsx), so hitting this is a
# recoverable state rather than a dead end -- which is what makes it safe to set at
# all. Without it, the send button is an open relay pointed at somebody's inbox.
max_frequency = "60s"

# What a guest session costs to mint, per hour, per address.
#
# A sign-in code is rate-limited by the mailbox it has to arrive at; a guest session has
# no such natural brake, so the ceiling has to be stated. 30 an hour per IP is GoTrue's
# own default and is far above real use -- a person presses the button once -- while
# keeping a script from filling `auth.users` from one machine faster than the sweep in
# 20260901190000 empties it.
#
# It is not the whole defence and should not be read as one: an address pool routes
# around it. The defences that do not care where the request came from are the ones in
# that migration -- a guest cannot enqueue generation, so the expensive door is shut
# regardless of how many sessions exist.
[auth.rate_limit]
anonymous_users = 30

# Second factor. GoTrue owns enrolment, challenge and verification; the recovery half
# -- codes that remove a lost factor -- is in 20260901150000, because Supabase does not
# provide one and a TOTP factor with no way back is a support ticket waiting to happen.
#
# TOTP only. Phone is off because this product deliberately asks for an email address
# and nothing else (docs/privacy.md), and turning on SMS would quietly make a phone
# number collectable. WebAuthn is off because it is not implemented in the UI, and an
# enabled factor type with no enrolment path is a way to strand an account.
[auth.mfa]
max_enrolled_factors = 10

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

# The default template renders `{{ .ConfirmationURL }}` and nothing else, so the
# email carries a link but no code — and `verifyOtp` has nothing to verify. The
# sign-in screen accepts either, and the code is the more reliable half: a link
# opened on a phone frequently lands in a different browser than the one that
# started the sign-in, and the session then arrives where nobody is looking.
#
# This configures the local stack only. The hosted project needs the same
# `{{ .Token }}` added under Authentication → Email Templates, or its codes keep
# arriving blank.
[auth.email.template.magic_link]
subject = "Your sign-in code"
content_path = "./supabase/templates/magic_link.html"

[edge_runtime]
enabled = true
policy = "oneshot"
inspector_port = 8083

# The Open Graph endpoint is read by link unfurlers — Slack, Twitter, iMessage —
# which send no Authorization header. Edge functions verify a JWT by default, so
# without this every crawler gets a 401 and the tags the function exists to
# render are never seen. It returns nothing that is not already public: the
# request goes through PostgREST as `anon`, so a draft or private summary is
# invisible to it exactly as it is to any other unauthenticated reader.
[functions.og]
verify_jwt = false

# The worker authenticates its own caller, against a token in Vault, because the
# platform cannot: the dispatcher is `pg_net` sending `x-worker-token` and no
# `Authorization` header at all. With JWT verification on, every tick is rejected
# before the function body runs and the queue simply stops draining — a 401 visible
# only in a log nobody is watching.
#
# Declared here rather than left to `--no-verify-jwt` on a command line, because a
# flag is a thing someone has to remember. Any deploy that forgets it — from CI, from
# memory, from `supabase functions serve` — silently re-enables verification and stops
# generation. This widens nothing: `authorised()` fails closed, so a worker with no
# credential configured still refuses every request.
[functions.worker]
verify_jwt = false
