{
  "name": "@wap/prompts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "baml:check": "baml check",
    "baml:generate": "baml generate && node -e \"const {existsSync,unlinkSync}=require('node:fs');const p='baml_sdk/.gitignore';if(existsSync(p))unlinkSync(p);if(existsSync(p))throw new Error('baml_sdk/.gitignore survived removal; it would hide the generated SDK from git')\"",
    "baml:test": "baml test",
    "baml:export": "node scripts/export.mjs"
  },
  "devDependencies": {
    "@boundaryml/baml-bridge": "0.17.0",
    "@types/node": "^26.4.0",
    "@wap/config": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
